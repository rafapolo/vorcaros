{
  "name": "vorcano",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.js",
    "dev": "bun run dev.js",
    "test": "bun run build.js && bunx playwright test",
    "test:ui": "bun run build.js && bunx playwright test --ui"
  },
  "dependencies": {
    "d3": "^7.9.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
